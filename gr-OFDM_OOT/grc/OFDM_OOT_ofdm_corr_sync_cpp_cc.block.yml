id: OFDM_OOT_ofdm_corr_sync_cpp_cc
label: ofdm_corr_sync_cpp_cc
category: '[OFDM_OOT]'

templates:
  imports: from gnuradio import OFDM_OOT
  make: OFDM_OOT.ofdm_corr_sync_cpp_cc(nfft=${nfft}, n_guard=${n_guard}, corr_sz=${corr_sz}, sync_corr_lvl=${sync_corr_lvl})

#  Make one 'parameters' list entry for every parameter you want settable from the GUI.
#     Keys include:
#     * id (makes the value accessible as keyname, e.g. in the make entry)
#     * label (label shown in the GUI)
#     * dtype (e.g. int, float, complex, byte, short, xxx_vector, ...)
#     * default
parameters:
- id: nfft
  label: nfft
  dtype: int
  default: 1024
- id: n_guard
  label: n_guard
  dtype: int
  default: 128
- id: corr_sz
  label: corr_sz
  dtype: int
  default: 64
- id: sync_corr_lvl
  label: corr lvl for detection
  dtype: float
  default: 0.8
#  Make one 'inputs' list entry per input and one 'outputs' list entry per output.
#  Keys include:
#      * label (an identifier for the GUI)
#      * domain (optional - stream or message. Default is stream)
#      * dtype (e.g. int, float, complex, byte, short, xxx_vector, ...)
#      * vlen (optional - data stream vector length. Default is 1)
#      * optional (optional - set to 1 for optional inputs. Default is 0)
inputs:
- label: in
  domain: stream
  dtype: complex
  optional: false

outputs:
- label: signal_bypass
  domain: stream
  dtype: complex
  optional: false
- label: correlation
  domain: stream
  dtype: complex
  optional: false

#  'file_format' specifies the version of the GRC yml format used in the file
#  and should usually not be changed.
file_format: 1
